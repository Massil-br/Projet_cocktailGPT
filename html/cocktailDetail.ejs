<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails du Cocktail</title>
    <%- include('partials/header') %>
</head>
<body class="bg-gradient-tropical min-vh-100 d-flex justify-content-center align-items-center">
    <div class="container mt-5">
        <h1 class="mb-4 text-center">Détails du Cocktail</h1>
        
        <% if (cocktail) { %>
            <div class="row">
                <div class="col-md-6">
                    <% if (cocktail.image) { %>
                        <img src="/<%= cocktail.image %>" class="img-fluid rounded" alt="<%= cocktail.name %>">
                    <% } else { %>
                        <div class="d-flex justify-content-center align-items-center bg-light border rounded" style="height: 300px;">
                            <span class="text-muted">Image non disponible</span>
                        </div>
                    <% } %>
                </div>
                <div class="col-md-6">
                    <h2><%= cocktail.name %></h2>
                    <p><strong>Type:</strong> <%= cocktail.alcohol === 'alcohol' ? 'Avec alcool' : 'Sans alcool' %></p>
                    
                    <div class="mt-3">
                        <h4>Description</h4>
                        <p><%= cocktail.description %></p>
                    </div>
                    
                    <div class="mt-3">
                        <h4>Ingrédients</h4>
                        <p><%= cocktail.ingredients %></p>
                    </div>
                    
                    <div class="mt-3">
                        <h4>Recette</h4>
                        <p><%= cocktail.recipe %></p>
                    </div>
                    <div class="mt-3">
                        <h4>Nom du skin associé</h4>
                        <% if(cocktail.api_skin_name) {%>
                                <p><%= cocktail.api_skin_name %></p>
                            </div>

                            <div class="col-md-6">
                            <% if (cocktail.api_skin_url) { %>
                                <img src="<%= cocktail.api_skin_url %>" class="img-fluid rounded" alt="<%= cocktail.api_skin_name %>">
                            <% } else { %>
                                <div class="d-flex justify-content-center align-items-center bg-light border rounded" style="height: 300px;">
                                    <span class="text-muted">Image non disponible</span>
                                </div>
                            <% } %>
                        </div>
                        <% } else{ %>
                            <p>Pas de cocktail associé</p>
                            </div>
                        <% } %>
                </div>
               
            </div>
            <div class="mt-4 text-center">
                <a href="/cocktailsList<%= research ? '?research=' + research : '' %>" class="btn btn-secondary">Retour à la liste</a>
            </div>
        <% } else { %>
            <div class="alert alert-danger">
                Cocktail non trouvé.
            </div>
            <div class="text-center">
                <a href="/cocktailsList<%= research ? '?research=' + research : '' %>" class="btn btn-secondary">Retour à la liste</a>
            </div>
        <% } %>
    </div>
</body>
</html>
