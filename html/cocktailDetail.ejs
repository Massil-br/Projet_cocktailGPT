<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails du Cocktail</title>
    <%- include('partials/header') %>
</head>
<body>
  <div class="details-wrapper">
    <% if (cocktail) { %>
      <div class="details-container">
        <h1 class="details-title">Détails du Cocktail</h1>

        <div class="details-content">
          <div class="visual-block">
            <% if (cocktail.image) { %>
              <img src="/<%= cocktail.image %>" class="details-image" alt="<%= cocktail.name %>">
            <% } else { %>
              <div class="image-placeholder">Image non disponible</div>
            <% } %>

            <% if (cocktail.api_skin_url) { %>
              <img src="<%= cocktail.api_skin_url %>" class="skin-image" alt="<%= cocktail.api_skin_name %>">
            <% } %>
          </div>

          <div class="details-info">
            <h2><%= cocktail.name %></h2>
            <p><strong>Type :</strong> <%= cocktail.alcohol === 'alcohol' ? 'Avec alcool' : 'Sans alcool' %></p>

            <div class="details-section">
              <h4>Description</h4>
              <p><%= cocktail.description %></p>

              <h4>Ingrédients</h4>
              <p><%= cocktail.ingredients %></p>

              <h4>Recette</h4>
              <p><%= cocktail.recipe %></p>

              <h4>Nom du skin associé</h4>
              <% if (cocktail.api_skin_name) { %>
                <p><%= cocktail.api_skin_name %></p>
              <% } else { %>
                <p>Pas de skin associé</p>
              <% } %>
            </div>
          </div>
        </div>

        <div class="details-return">
          <a href="/cocktailsList<%= research ? '?research=' + research : '' %>" class="btn-fortnite">← Retour à la liste</a>
        </div>
      </div>
    <% } else { %>
      <div class="alert alert-danger">Cocktail non trouvé.</div>
      <div class="details-return">
        <a href="/cocktailsList<%= research ? '?research=' + research : '' %>" class="btn-fortnite">← Retour à la liste</a>
      </div>
    <% } %>
  </div>
</body>
</html>
