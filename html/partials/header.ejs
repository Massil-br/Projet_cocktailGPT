<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="Style.css">
</head>
<body class="body-head">
  <nav class="navbar">
    <!-- Gauche : Logos -->
    <div class="nav-left">
      <a href="/" class="nav-link">CocktailNite 🍹</a>
    </div>

    <!-- Centre : Navigation -->
    <div class="nav-center">
      <a href="/" class="nav-link">Accueil</a>
      <a href="/cocktailsList" class="nav-link">Cocktails</a>
      <a href="/contact" class="nav-link">Contact</a>

      <% if (user && (user.role == "admin" || user.role == "owner")) { %>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">Admin</a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="/admin/users">Users</a></li>
            <li><a class="dropdown-item" href="/admin/cocktails">Cocktails</a></li>
          </ul>
        </div>
      <% } %>
    </div>

    <!-- Droite : Auth & recherche -->
    <div class="nav-right">
      <% if (user && user.username) { %>
        <span class="username">👋 <%= user.username %></span>
        <a href="/auth/logout" class="btn btn-primary">Déconnexion</a>
      <% } else { %>
        <a href="/register" class="btn btn-secondary">Inscription</a>
        <a href="/login" class="btn btn-primary">Connexion</a>
      <% } %>

      <form class="search-form" action="/cocktailsList" method="GET">
        <input class="search-input" type="search" name="research" placeholder="Recherche" />
        <button class="btn btn-warning" type="submit">Rechercher</button>
      </form>
    </div>
  </nav>
</body>
</html>

